---
trigger: always_on
---

Read the project documentation before coding.
Do not change the folder structure without a clear technical reason.
Do not use `any`.
Do not hardcode UI text.
Use `next-intl` for all UI text.
Use `Zod` for validation.
Use Prisma for database access.
Use Supabase Auth for authentication.
Use Supabase Storage for report photos.
Do not store tokens or secrets on the client.
Run lint and build after completing major tasks.
Make small, measured changes.
When finished, document the files you changed, the reasons for the changes, and the verification results.
